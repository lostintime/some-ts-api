import { expect } from "chai"

describe("API", () => {
  describe("Breaking Changes", () => {
    it("didn't happen yet", () => {
      // Breaking changes test is coming here, after first version is published
      expect(true).not.equals(false, "there is only one true")
    })
  })
})
